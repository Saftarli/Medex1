version: '3.12'
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - Medex1/docker.env
    volumes:
      - .:/srv/app
    ports:
      - '0.0.0.0:4000:4000'
    command: python manage.py runserver 0.0.0.0:4000

volumes:
  static-data:


